fragment Theme on GameTheme {
  id
  name
  description
  characterImage {
    results {
      fileUrl
      fileName
    }
  }
}

query GetAllThemesQuery {
  allGameTheme {
    results {
      ...Theme
    }
  }
}
