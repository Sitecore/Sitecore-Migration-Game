fragment OutcomeDetails on Outcome {
  id
  name
  themes {
    results {
      ... on Theme {
        id
        name
      }
    }
  }
  title
  productsIntro
  outcomeReasons {
    results {
      ... on OutcomeReason {
        product
        reason
      }
    }
  }
  videoTitle
  videoIntro
  videoid
  guidesIntro
  xcFeaturesTitle
  xcFeaturesIntro
  xpFeaturesTitle
  xpFeaturesIntro
  xmFeaturesTitle
  xmFeaturesIntro
  aspnetHeadlessTitle
  aspnetHeadlessIntro
}

query GetOutcomeByThemeIdQuery($id: ID!) {
  allOutcomes(where: { themes: { gameOutcome_ids: $id } }) {
    results {
      ...OutcomeDetails
    }
  }
}

query GetOutcomeByIdQuery($id: String!) {
  outcome(id: $id) {
    ...OutcomeDetails
  }
}
