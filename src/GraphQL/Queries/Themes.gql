fragment Theme on GameTheme {
  id
  name
  description
  characterImage {
    results {
      fileUrl
      fileName
    }
  }
}

query GetAllThemesQuery {
  allGameTheme {
    results {
      ...Theme
    }
  }
}

query GetThemesByIdQuery($id: ID!) {
  gameTheme(id: $id) {
    ...Theme
  }
}
